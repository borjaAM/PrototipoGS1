<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.ulpgc.gs1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Easytherapist">
        <activity android:name=".view.patient.ModifyTreatmentPlanActivity"></activity>
        <activity android:name=".view.patient.CreateTreatmentPlanActivity" />
        <activity android:name=".view.patient.ModifyPatientsActivity" />
        <activity android:name=".view.patient.CreatePatientsActivity" />
        <activity android:name=".view.patient.PatientListActivity" />
        <activity android:name=".view.userView.MyProfileActivity" />
        <activity android:name=".view.userView.MainMenuActivity" />
        <activity android:name=".view.loginAndregister.signInActivity" />
        <activity android:name=".view.loginAndregister.LogInActivity" />
        <activity android:name=".view.loginAndregister.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>